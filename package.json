{
  "name": "callput",
  "private": true,
  "workspaces": [
    "shared",
    "backend/*",
    "frontend",
    "callput-agent-mcp"
  ],
  "packageManager": "npm@10.3.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}